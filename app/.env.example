# Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Set to 1 during local dev to render the fixture cards instead of reading Supabase.
# Once you've ingested real feed_cards, set to 0.
NEXT_PUBLIC_USE_FIXTURES=1
